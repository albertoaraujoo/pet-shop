pre-commit:
  commands:
    format:
      run: npm run format -- {staged_files}
      glob: '*.{js,jsx,ts,tsx,css,json,md,prisma,mjs,yml,yaml}'
      stage_fixed: true

pre-push:
  commands:
    validate:
      glob: '*.{ts,tsx}'
      run: npm run validate:typecheck
